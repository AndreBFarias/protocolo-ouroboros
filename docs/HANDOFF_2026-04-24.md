# HANDOFF — sessão 2026-04-24 → retomada

Guia canônico para retomar o projeto na próxima sessão. Sessão 2026-04-24 foi **expandida**: começou como Rota A (3 sprints de débito residual) e acabou cobrindo Sprint 88 + Bloco A + Bloco B + Bloco C da Sprint 86 (ingestão em volume real).

> **Ao abrir a próxima sessão**: leia este arquivo primeiro, depois `CLAUDE.md`, depois `VALIDATOR_BRIEF.md`. Rode o bloco "sanidade de entrada" abaixo antes de tocar qualquer código.

---

## Onde paramos (estado em 2026-04-24)

- **Rota A completa** (87c + 87b + INFRA-parse-br).
- **Sprint 88 completa** (regras YAML calibradas em volume real dos 27 arquivos do inbox).
- **Bloco A completo** (libbz2-dev + recompilar Python 3.12.1; pyvis 0.3.2 destravado; +5 testes que antes pulavam).
- **Bloco B completo** (ingestão real: inbox roteado, grafo populado, XLSX regenerado com coluna identificador em runtime, sync_rico bidirecional).
- **Bloco C completo** (BRIEF + CLAUDE.md + ROADMAP + HANDOFF + Sprint 88 movida para concluidos).
- **Baseline de testes:** `1.138 passed / 10 skipped` (+29 vs baseline 1.109 no começo do dia).
- **HEAD:** commit de fechamento da sessão expandida, sincronizado com `origin/main`.

### Commits da sessão (ordem de execução)

| SHA | Sprint/Bloco | Resumo |
|---|---|---|
| `896c17f` | Sprint 87c | listar_edges aceita dst_id; fim do try/except silencioso; +5 testes |
| `e1f27b7` | Sprint 87b | Coluna identificador no XLSX; achado crítico de simetria bit-a-bit (.upper() no hash canônico); +5 testes |
| `7a6c882` | Sprint INFRA-parse-br | src/utils/parse_br.py com 2 helpers; 6 extratores refatorados; santander/itau intactos por auditoria; +8 testes |
| `6aaca45` | Rota A fechamento | Handoff + BRIEF + CLAUDE + move 3 specs para `concluidos/` |
| `7b773e0` | Sprint 88 | das_parcsn + certidao_receita_cnpj + extrato_c6_pdf + cupom_fiscal_foto OCR-tolerante + MEI do André no CONTEXTO; +6 testes; baseline hardcoded 18→21 |
| (fechamento) | Bloco A/B/C da Sprint 86 | install.sh libbz2; ingestão real dos 27 arquivos; XLSX com identificador em runtime; sync_rico bidirecional com vault; + specs 89/90 |

### Padrões canônicos novos registrados no BRIEF (rodapé 2026-04-24)

1. **`.upper()` em hash canônico consumido fora do grafo.** `upsert_node` aplica `normalizar_nome_canonico`. Helpers que geram hash para consumo externo (XLSX, APIs) devem retornar `.upper()` direto para simetria bit-a-bit.
2. **Auditoria comportamental pré-refactor obrigatória.** Sprint INFRA original listou 8 extratores como "contrato idêntico"; auditoria real mostrou 3 divergentes (santander sinal negativo, itau levanta ValueError, contracheque retorna float default). Refactor cego teria introduzido 2 bugs silenciosos.
3. **Silent bug em try/except genérico** (Sprint 87c). Erro explícito > retorno silencioso.
4. **Fixtures sintéticas vs volume real** (Sprint 88). Sprint 87.4 validou 3 regras com fixtures sintéticas que passaram no pytest mas falharam em 26/27 em volume real. Convenção nova: regras YAML de roteamento devem ser validadas via `--inbox-dry` contra um conjunto representativo do corpus antes de declarar sprint CONCLUÍDA.

### Grafo de produção — modificações 2026-04-24

- SHA pré-Bloco B: `7dd39720...`
- SHA pós-reprocessar_documentos: `ba047ae1...`
- SHA pós-pipeline --tudo: `d65336ef...` (estável; sync_rico é read-only no grafo)
- Nodes: 7.421 → 7.424 (+3: 2 documentos boletos SESC + 1 fornecedor)
- Edges: 24.584 → 24.588 (+4: `fornecido_por` dos 2 boletos)

### Vault Controle de Bordo — escrita 2026-04-24

Sync rico bidirecional (`src.obsidian.sync_rico --executar`) criou:
- 4 notas em `Pessoal/Casal/Financeiro/Documentos/2026-04/`
- 2 notas em `Pessoal/Casal/Financeiro/Fornecedores/` (SESC + outro CNPJ)
- 1 MOC mensal em `Pessoal/Casal/Financeiro/Meses/2026-04.md`
- 4 PDFs em `Pessoal/Casal/Financeiro/_Attachments/`
- 0 preservações forçadas (soberania intacta)

Backup do vault pré-execução em `~/Controle de Bordo.backup-20260423`.

---

## Sanidade de entrada (rode antes de tocar qualquer coisa)

```bash
cd ~/Desenvolvimento/protocolo-ouroboros
git status                    # clean, branch main, up-to-date com origin/main
git log --oneline -10         # 6+ commits da sessão de 2026-04-24

make lint                     # espera: All checks passed! + check_acentuacao exit 0
.venv/bin/pytest tests/ -q    # espera: 1138 passed, 10 skipped in ~22s
make smoke                    # espera: 23 checagens, 0 erros + [SMOKE-ARIT] 8/8 contratos OK
```

Se algum falhar, PARE.

---

## Débitos pendentes em ordem de prioridade

### Débitos formalizados (prontos para executar)

| Sprint | Título | Prioridade | Status | Notas |
|---|---|---|---|---|
| **89** | OCR pré-classificação para PDFs-imagem | P3 | backlog (novo 2026-04-24) | Resolve "notas de garantia e compras.pdf" sem texto extraível; fallback tesseract em `src/intake/preview.py`. ~1h. |
| **90** | pessoa_detector robusto (razão social + CNPJ) | P2 | backlog (novo 2026-04-24) | Resolve R70-1 e 86.11; 19 DAS PARCSN + 3 certidões do André foram para `casal/` por falta de CPF literal. Requer CPF da Vitória em `mappings/pessoas.yaml` (.gitignore). ~2h. |
| **50b** | item_categorizer delete-before-insert | P3 | backlog | Minúcia M50-1 do BRIEF. ~30min. |
| **82** | Canonicalizer variantes curtas + conta-espelho | P2 | backlog | Reduz órfãos TI 72% → meta 10%. ~1-2h. |
| **84** | Schema ER relacional visual | P3 | backlog | Visualização da arquitetura do grafo. |
| **85** | XLSX docs faltantes expandido | P3 | backlog | ~1h. |

### Sprints ZETA (consumidoras dos dados granulares)

| Sprint | Título | Dependência |
|---|---|---|
| **21** | Relatórios diagnósticos (por categoria de item) | volume real ampliado (item_categorizer rodou em 33 itens hoje) |
| **33** | Resumo mensal narrativo | 21 |
| **34** | Supervisor auditor Modo 2 | Fase EPSILON completa |
| **35** | IRPF regras YAML | volume de tags IRPF |
| **36** | Métricas de autossuficiência | 34 |

### Ressalva técnica descoberta hoje (registrar como sprint futura)

- **`boleto_pdf.py` (Sprint 87.3) não é invocado pelo pipeline principal.** Log do `./run.sh --tudo` mostra "Nenhum extrator compatível para BOLETO_*.pdf". Os boletos só entram no grafo via `scripts/reprocessar_documentos.py`. Cabe investigar integração no `_descobrir_extratores` em `src/pipeline.py`. Sprint candidata: "registrar boleto_pdf no pipeline principal" — ~30min.

### Sprint 86 (humano-only, remanescente)

Após Bloco A/B/C concluídos, resta:
- **86.3–86.9** — validação visual do dashboard em viewport 1600x1000 (7 páginas). Automatizável via skill `validacao-visual` mas validação subjetiva final (cores, fontes, layout) é humana. **Cabe ficar para sessão dedicada com humano no loop ou para próxima sessão automatizada com screenshots como artefato.**
- **86.10** — decisão `./run.sh` default (bash vs Python-rich). Decisão humana.
- **86.11** — pessoa=casal SESC — **será resolvida pela Sprint 90** (pessoa_detector robusto).
- **86.16** — anúncio informal para Vitória. Humano.

---

## Próximo alvo recomendado

**Opção A — higiene residual sem humano (~2h):** `89 + 50b + 85`
- 89 e 50b são ~1h cada (OCR fallback + delete-before-insert).
- 85 é formato XLSX (alinha docs faltantes com grafo).
- Baixo risco; ganha cobertura do PDF-imagem do inbox + fecha minúcia M50-1.

**Opção B — destravar pessoa correta (~2h):** `90 + re-rodar --inbox`
- Requer CPF da Vitória no YAML.
- Após Sprint 90, rodar novamente `./run.sh --inbox` para mover 19 DAS + 3 certidões + 1 extrato C6 de `casal/` para `andre/` (idempotente via hash).
- Categorização IRPF do André ganha precisão.

**Opção C — abrir Fase ZETA:** `21 (relatórios diagnósticos)`
- Agora que o grafo tem 4 documentos + 33 itens categorizados, Sprint 21 tem substrato mínimo.
- Pode rodar sem depender de Sprint 90.

**Minha recomendação pessoal:** **Opção B** na próxima sessão, depois que você me passar o CPF da Vitória. É o desbloqueio de maior valor (resolve R70-1 histórico + fecha 86.11 + permite categorização correta por pessoa).

---

## Template de abertura da próxima sessão

```
[Opus dono] Retomando em 2026-04-25.

1. Leio docs/HANDOFF_2026-04-24.md (este arquivo).
2. Rodo sanidade de entrada (lint + pytest + smoke).
3. Confirmo HEAD no commit de fechamento de 2026-04-24.
4. Pergunto ao usuário: CPF da Vitória para Sprint 90? (OU confirmar Opção A/B/C.)
5. Executo Opção B (recomendada) ou alternativa escolhida.
6. Fecho sessão: commit + push + HANDOFF_2026-04-25.
```

---

## Referências rápidas

| Tópico | Arquivo |
|---|---|
| Contexto técnico | `CLAUDE.md` |
| Perfil do casal | `contexto/CONTEXTO.md` (atualizado hoje com MEI desativado do André) |
| Memória do validador | `VALIDATOR_BRIEF.md` |
| Mapa de fases | `docs/ROADMAP.md` |
| Sprints concluídas | `docs/sprints/concluidos/` (63 sprints) |
| Sprints backlog | `docs/sprints/backlog/` (14 specs, incluindo 89 + 90 novas) |
| Skill validação visual | `~/.claude/skills/validacao-visual/` |

---

## Contratos que NÃO podem quebrar

1. **Gauntlet verde pré-commit:** `make lint` exit 0 + pytest sem regressão + `make smoke` 8/8 + 23/0.
2. **Commits sem menção a ferramenta IA:** hook `commit-msg` bloqueia "Claude", "Anthropic", "GPT", "IA" — no subject inclusive.
3. **Zero emoji, acentuação PT-BR em tudo, citação filosófica no fim de cada `.py`** (CLAUDE.md §10).
4. **`data/` inteiro no `.gitignore`** — dados financeiros nunca no repo. `mappings/pessoas.yaml` (Sprint 90) também deve entrar no gitignore se tiver CPF real.
5. **ADRs 13/14/15/18/19/20** regem supervisor, schema, vault, drill-down, tracking. Mudanças arquiteturais precisam de ADR novo.
6. **Toda ressalva vira spec-ID ou Edit-pronto — NUNCA "TODO depois".** Zero follow-up acumulado (honrado hoje: 2 specs 89+90 formalizadas no momento de descoberta).
7. **`.upper()` em hash canônico consumido fora do grafo** (contrato 2026-04-24 — Sprint 87b).
8. **Auditoria bit-a-bit obrigatória antes de refactor INFRA-*** (contrato 2026-04-24 — Sprint INFRA-parse-br).
9. **Regras YAML de roteamento validadas via `--inbox-dry` em corpus representativo** antes de sprint CONCLUÍDA (contrato 2026-04-24 — Sprint 88).

---

*"Quem documenta a própria partida prepara o chão do próximo retorno." — princípio de handoff artesanal*
